# v2.0.0 (Draft) — Identity Registrar Consolidation

## Status
Draft release notes for the single-contract architecture migration.
Mainnet deployment for `FreeTrialSubdomainRegistrarIdentity` is pending until a real deployment artifact is produced.

## Highlights
- Standardizes production flow on exactly one active contract: `FreeTrialSubdomainRegistrarIdentity`.
- Root-scopes the system to `alpha.agent.agi.eth` (`ROOT_NODE` hard-coded in contract).
- Uses one atomic path for wrapped subname registration + soulbound identity mint.
- Keeps lifecycle repair paths (`claimIdentity`, `syncIdentity`, `syncIdentityByLabel`) for stale/desynced state.
- Aligns toolchain on Hardhat 2.x, TypeScript, Solidity 0.8.24 (optimizer 200, viaIR false), Node 20.19.6.
- Removes legacy registrar from active compile/deploy/operator workflows.

## Historical reference
- Legacy historical release (`v1.0.0`): `FreeTrialSubdomainRegistrar` at `0x7aAE649184182A01Ac7D8D5d7873903015C08761`.
- This legacy deployment remains documentation-only and is not part of active runbooks.

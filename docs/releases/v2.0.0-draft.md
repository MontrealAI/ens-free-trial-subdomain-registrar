# v2.0.0 (Draft) — Identity Primitive Flagship

## Status
Draft release notes for next major release. Mainnet deployment for `FreeTrialSubdomainRegistrarIdentity` is pending.

## Highlights
- Introduces `FreeTrialSubdomainRegistrarIdentity` as new flagship contract.
- Adds atomic wrapped subname + soulbound NFT mint path.
- Adds claim/sync identity lifecycle functions for migration/backfill and desync/expiry handling.
- Preserves legacy `FreeTrialSubdomainRegistrar` and existing v1.0.0 live deployment docs.
- Updates deploy/verify scripts to support both `identity` and `legacy` modes (legacy remains the default mode for conservative operator safety).
- Upgrades compiler baseline to solc `0.8.24` (optimizer enabled, runs 200, viaIR false).

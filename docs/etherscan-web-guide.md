# Etherscan Web Guide (Historical)

This guide is retained only as historical context for legacy contract interactions.

Active production operations for this repository are script-driven and target only:
- `FreeTrialSubdomainRegistrarIdentity`

Use the verified address from the real deployment artifact:
- `release-assets/mainnet-free-trial-subdomain-registrar-identity.json`
